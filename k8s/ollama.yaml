# ============================================================================
# DISABLED — DO NOT APPLY ON CURRENT 2GB VDS.
#
# This manifest deploys Ollama in-cluster for self-hosted LLM inference.
# It was tested with qwen2.5:1.5b but doesn't fit on the current 2GB VDS:
# scheduler returns "Insufficient memory" because Ollama requests 1200Mi
# while other services (backend, frontend, mongodb, redis, minio, 2x worker)
# already consume most of the available RAM.
#
# Worker is currently configured to use Groq (managed LLM provider) — see
# k8s/translation-worker.yaml ConfigMap.
#
# To re-enable Ollama:
#   1. Provision a VDS with >= 4GB RAM
#   2. Apply this manifest: kubectl apply -f k8s/ollama.yaml
#   3. Switch worker back to Ollama: edit ConfigMap in translation-worker.yaml:
#        LLM_BASE_URL: "http://ollama:11434/v1"
#        LLM_MODEL: "qwen2.5:1.5b"
#      and update LLM_API_KEY secret to "ollama" (any non-empty string).
# ============================================================================
# k8s/ollama.yaml
# Ollama (LLM inference) for translation-worker.
# Model: qwen2.5:1.5b — ~400 MiB on disk, ~600-700 MiB at inference time on CPU.
# First boot pulls the model into PVC; subsequent restarts are instant (no-op pull).
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: default
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 3Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  namespace: default
  labels:
    app: ollama
spec:
  replicas: 1
  strategy:
    type: Recreate          # PVC is RWO — two pods cannot mount it simultaneously
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      containers:
        - name: ollama
          image: ollama/ollama:latest
          ports:
            - containerPort: 11434
              name: http
          # Start the server, wait for it, then pull the model and keep server in foreground.
          # `ollama pull` is idempotent: if the blob is already in /root/.ollama it returns instantly.
          command: ["/bin/sh", "-c"]
          args:
            - |
              ollama serve &
              SERVE_PID=$!
              # Wait for server to accept requests (max ~30s).
              for i in $(seq 1 30); do
                if ollama list >/dev/null 2>&1; then break; fi
                sleep 1
              done
              ollama pull qwen2.5:1.5b
              wait $SERVE_PID
          env:
            - name: OLLAMA_HOST
              value: "0.0.0.0:11434"
            - name: OLLAMA_KEEP_ALIVE
              value: "30m"
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
          readinessProbe:
            httpGet:
              path: /api/tags
              port: 11434
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 30        # generous window for first-time model pull
          livenessProbe:
            httpGet:
              path: /api/tags
              port: 11434
            initialDelaySeconds: 120
            periodSeconds: 30
          resources:
            requests:
              cpu: "200m"
              memory: "1200Mi"
            limits:
              cpu: "1500m"
              memory: "2Gi"
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: ollama-models
---
apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: ollama
  ports:
    - name: http
      port: 11434
      targetPort: 11434